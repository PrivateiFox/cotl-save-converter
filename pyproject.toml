[project]
name = "cotl-save-converter"
version = "0.1.0"
description = "Convert Cult of the Lamb save files between iOS (ZB+gzip) and Steam (AES-CBC) formats"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "cryptography>=46.0.7",
    "lz4>=4.4.5",
    "msgpack>=1.1.2",
]
